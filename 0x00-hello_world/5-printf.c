#include <stdio.h>
/**
 * main - the main function is to use printf
 * Return = 0, this programme should return 0 (Success)
 *
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
